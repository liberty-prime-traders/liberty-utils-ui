services:
  daily-snapshot-ui:
    image: ezraorina834/daily-snapshot-ui:latest
    container_name: daily-snapshot-ui
    restart: unless-stopped
    networks:
      - ezra-home

networks:
  ezra-home:
    external: true
